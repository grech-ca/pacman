export enum Color {
  Black = '#000',
  White = '#fff',
  Blue = '#00f',
  Orange = '#fa0',
  Yellow = '#ff0',
  Purple = '#f0a',
  Green = '#0f0',
  Red = '#f00',
  Cyan = '#0ff'
}

export enum Arrow {
  Up = 'ArrowUp',
  Down = 'ArrowDown',
  Left = 'ArrowLeft',
  Right = 'ArrowRight',
}

export enum Direction {
  Up,
  Down,
  Left,
  Right,
  None
}

export enum LevelObject {
  Air,
  Wall,
  Food,
  Player,
  Ghost
};
